# File Transfer

## SCP

### Local → Remote

```bash
scp <local_file_folder> <remote_username>@<remote_ip>:<remote_directory>
```

### Remote → Local

```bash
scp <remote_username>@<remote_ip>:<remote_file_folder> <local_directory>
```
